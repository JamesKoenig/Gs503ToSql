#ifndef _CONNECTION_MAIN_LOOP_HEADER_
#define _CONNECTION_MAIN_LOOP_HEADER_
#include "connection.h"

//what the connection executes as a loop
int connectionLoop(Connection * con);

#endif /* _CONNECTION_MAIN_LOOP_HEADER_ */
